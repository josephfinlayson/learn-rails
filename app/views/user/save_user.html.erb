<%= form_for @user, url: {action: "save_user"} do |f| %>
    <%= f.text_field(:name) %>
    <%= f.label_tag(:name, "name") %>
    <%= f.text_field(:image) %>
    <%= f.label_tag(:image, "image") %>
    <%= f.submit "Create User" %>
<% end %>